package com.oceanbase.powermem.sdk.core;

/**
 * Audit logger for memory operations (pure Java core migration target).
 *
 * <p>In Python, audit logging is wired into core operations (add/search/get/update/delete). This Java
 * placeholder exists to host the same behavior in a Java-native implementation.</p>
 *
 * <p>Python reference: {@code src/powermem/core/audit.py}</p>
 */
public class AuditLogger {
}

